# FractionTest 제작 핸드아웃

## 1. 프로젝트 목적

기존 `ClockTest`의 화면 구성과 운영 기능을 바탕으로 초등학생용 분수 문제 앱을 별도로 제작한다.

- 원본 `ClockTest` 폴더와 기능은 그대로 유지한다.
- 새 앱은 `FractionTest` 폴더에서 독립적으로 개발한다.
- 시계 문제와 시계 전용 입력·표시만 분수 문제에 맞게 교체한다.
- 로그인, 학생 관리, 결과 저장, 선생님 조회 등 기존 운영 흐름은 가능한 한 동일하게 유지한다.

## 2. 현재 상태

- `ClockTest/FractionTest` 폴더에 원본 앱의 기본 파일만 복사돼 있다.
- 분수 문제 생성, 입력, 채점 기능은 아직 구현하지 않았다.
- 원본 `ClockTest` 소스와 GitHub 저장소에는 분수 앱 변경이 적용되지 않았다.
- 아래 출제 규칙이 확정되기 전에는 임의로 문제 유형을 구현하지 않는다.

## 3. 유지할 기능

### 학생 화면

- 학생 이름 입력
- `창동 / 하계 / 중계` 반 선택
- 학생용 비밀번호 확인
- DB에 등록되지 않은 학생일 때 신규 생성 확인 팝업
- 신규 생성 승인 시 TES 학원과 선택 반에 학생 연결
- 난이도 또는 학습 단계 선택
- 20문제 응시 흐름
- 문제 진행률 표시
- 숫자 키패드 입력
- 정답·오답 즉시 피드백과 효과음
- 나가기 확인 팝업
- 결과 자동 저장과 저장 재시도
- 결과표, 정답 목록, 오답 목록
- 다시 풀기와 단계 선택으로 이동
- 가로 화면 안내와 전체화면 기능

### 선생님 화면

- 구글 로그인은 사용하지 않는다.
- 선생님 공통 비밀번호로 1차 로그인
- `창동 / 하계 / 중계` 중 한 반 선택
- 선택한 반의 비밀번호로 2차 로그인
- 해당 반 학생의 분수 응시 결과만 조회
- 날짜 필터와 학생 이름 검색
- 학생별 상세 이력과 공유 링크
- 새로고침과 로그아웃

## 4. 분수 앱으로 변경할 부분

- 앱 이름과 문구: 시계 관련 표현을 분수 학습 표현으로 변경
- 문제 생성기: 시각 읽기 문제를 분수 문제 생성기로 교체
- 문제 표시 영역: 시계 SVG를 분수식 또는 분수 그림으로 교체
- 입력 영역: 시·분·초 입력을 분수 답안 형식에 맞게 교체
- 채점 방식: 시각 비교를 분수 답안 비교로 교체
- 결과표: 시계 답안 형식을 분수 형식으로 표시
- 공유 성적표: 분수 문제, 정답, 학생 답을 분수 형식으로 표시
- 선생님 통계: 시계 레벨과 시간 표현을 분수 단계와 분수 답안으로 변경
- Supabase 결과 구분값을 시계 앱과 분리
  - `book_title`: `TES 분수`
  - `unit_title`: 분수 앱 전용 단원 ID
  - 시계 앱의 `TES 시계`, `CLOCK_L...` 결과와 섞이지 않게 조회
- PWA 이름, 설명, 브라우저 제목과 아이콘을 분수 앱에 맞게 변경
- 로컬 저장소 키를 시계 앱과 분리해 같은 브라우저에서도 데이터가 충돌하지 않게 변경

## 5. 확정이 필요한 출제 규칙

다음 항목은 사용자가 정한 뒤 구현한다.

1. 학년 또는 대상 수준
2. 전체 단계 수와 단계별 문제 유형
3. 진분수, 가분수, 대분수 포함 범위
4. 분수의 크기 비교 포함 여부
5. 약분과 통분 포함 여부
6. 덧셈·뺄셈·곱셈·나눗셈 포함 범위
7. 분모와 분자의 최댓값
8. 답을 반드시 기약분수로 입력해야 하는지
9. 동치분수를 정답으로 인정할지
10. 대분수 답안 입력 형식
11. 문제당 제한시간 또는 전체 제한시간 사용 여부
12. 단계별 문제 수가 모두 20문제인지

## 6. 입력 UX 원칙

- 대상은 초등학생이므로 빠른 연속 입력을 강요하지 않는다.
- 분자·분모·자연수 부분이 필요하면 입력칸을 명확히 분리한다.
- 숫자 키패드 구조와 큰 터치 영역을 유지한다.
- 출제 범위를 입력 제한으로 사용하지 않는다.
- 형식상 입력 가능한 값은 받은 뒤 정답 여부로 채점한다.
- 입력 도중 자동 제출로 오답 처리되지 않게 한다.
- `입력` 버튼 또는 명확한 완료 동작으로 제출한다.
- 0인 분모처럼 수학적으로 성립하지 않는 형식만 차단한다.

## 7. 데이터 연결 원칙

- 학생의 반과 학원을 다른 학생 데이터로 추론하지 않는다.
- 기존 학생은 `classes.student_ids`에 포함된 UUID로 연결한다.
- 신규 학생은 확인 팝업에서 승인된 경우에만 생성한다.
- 결과의 `student_id`에는 실제 학생 UUID를 저장한다.
- 분수 결과는 시계 결과와 `book_title`, `unit_title`로 명확히 분리한다.
- 실제 저장 성공 ID를 확인하기 전에는 관리자 필터가 정상이라고 판단하지 않는다.
- 스키마나 RLS 변경이 필요하면 사용자 승인 후에만 진행한다.

## 8. 구현 순서

1. 사용자가 단계별 분수 출제 규칙을 확정한다.
2. 분수 문제·정답 데이터 구조를 설계한다.
3. 문제 생성기와 채점 테스트를 먼저 작성한다.
4. 문제 표시와 답안 입력 UI를 구현한다.
5. 게임 화면과 결과 화면을 분수용으로 변경한다.
6. Supabase 저장 구분과 관리자 조회를 분수용으로 변경한다.
7. 공유 성적표와 학생별 통계를 변경한다.
8. 실제 등록 학생과 신규 학생으로 각각 저장을 검증한다.
9. 반별 관리자 조회와 공유 링크를 검증한다.
10. 타입 검사, 프로덕션 빌드, 태블릿 가로 화면을 최종 확인한다.

## 9. 완료 기준

- 원본 `ClockTest`가 변경 없이 계속 실행된다.
- `FractionTest`가 별도 앱으로 실행되고 빌드된다.
- 확정된 모든 분수 단계가 중복·오류 없이 출제된다.
- 학생 입력이 의도한 분수 형식으로 채점된다.
- 등록 학생과 승인된 신규 학생의 결과가 Supabase에 저장된다.
- 선생님 페이지에서 선택한 반의 분수 결과만 조회된다.
- 결과표와 공유 링크에서 문제·정답·학생 답이 정확한 분수로 표시된다.
- 시계 앱 결과와 분수 앱 결과가 서로 섞이지 않는다.
